# Aggregation Expression Builder

The aggregation expression builder creates expressions used in aggregation pipelines. The categories follow `MongoDB` aggregation expression docs and only list expressions implemented by the `aggregation` package.
